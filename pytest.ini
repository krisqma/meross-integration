[pytest]
testpaths = tests
markers =
    integration: testy wymagające uruchomionego stacku Docker (broker, most, atrapy)
    hardware: testy dotykające prawdziwych gniazdek Meross w LAN-ie
addopts = -m "not hardware and not integration"
asyncio_mode = auto
